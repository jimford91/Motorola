spring:
  application:
    name: "motorola"

management:
  datadog:
    metrics:
      export:
        enabled: false

storage:
  file:
    base-location: "src/test/resources/uploadFiles"
    request-timeout: 5